# sprint-todo
`docker-compose up -d`
`docker-compose exec php bash`
`composer create-project --prefer-dist  laravel/laravel . "9.*"`
`composer require laravel/ui "4.*" --dev`
`php artisan ui bootstrap`
`npm install && npm run dev`

 chown -R 33:33 "/root/.npm"