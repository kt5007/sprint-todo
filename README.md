# sprint-todo
`docker-compose up -d`
`docker-compose exec php bash`
`composer create-project --prefer-dist  laravel/laravel . "9.0.0"`
`composer require laravel/ui ^4.2`
`php artisan ui bootstrap`
`npm install && npm run dev`